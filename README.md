# 3dWorld-for-java

This is Eclipse project to import into workspace. It renders 3d objects on window.

Many objects included - just go into resources subdirectory.

Try F1 - F8 keys for textures, shading, etc.

Use arrow keys to move object and A, W, S, D to rotate it (or mouse so to speak).
